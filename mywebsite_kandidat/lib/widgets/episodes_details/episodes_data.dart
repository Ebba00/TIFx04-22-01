import 'package:flutter/material.dart';

class EpisodesData extends StatelessWidget {
  const EpisodesData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 200,
      color: Colors.deepOrange,
    );
  }
}